# DSH read-only SQLite Plugin reviewer continuation case

Continue the DSH SQLite Plugin E2E in the single Git worktree `/Users/zengjice/Projects/deepseek-harness-sqlite-e2e`. The user explicitly cancelled deadline-blocked Run `at-dsh-sqlite-plugin-v4pro-20260826`, which had continued earlier interrupted Runs. Its Developer completed a Turn and routed the candidate to Reviewer, but the Reviewer was stopped by the 7200-second hard deadline before issuing any verdict. Preserve the complete uncommitted live diff, but treat every predecessor claim, test result, and partial review as untrusted work material until independently reproduced.

Use three independent DeepSeek Harness External Agents: Reviewer, Developer, and a fresh Validator. Reviewer is the initial role. Reviewer and Developer use resumable Sessions within this new Run; Validator uses a fresh Session. All roles must use `deepseek-official/deepseek-v4-pro-ga-260813` and record observed runtime model evidence. Resolve endpoint and credentials only through DeepSeek Harness's native trusted environment; never persist environment values.

The candidate is an opt-in, model-facing Plugin named `@deepseek-ai/dsh-tool-sqlite` at `packages/storage/tool-sqlite`. It exposes `sqlite_schema` for inspecting workspace-authorized local SQLite tables, views, columns, primary keys, foreign keys, and indexes, plus `sqlite_query` for exactly one parameterized read-only `SELECT`, `WITH ... SELECT`, or `EXPLAIN QUERY PLAN` statement. Results must be typed and bounded, report elapsed time and explicit truncation, preserve integers beyond JavaScript's safe range, and summarize BLOB values without returning bytes.

Read-only behavior must be enforced by both read-only SQLite connection mode and an engine-level authorizer, never SQL text matching alone. Reject DML, DDL, `ATTACH`, `DETACH`, `VACUUM`, writable `PRAGMA`, extension loading, transactions, and multiple statements. Database paths must stay inside the authorized workspace and resist absolute paths, parent traversal, and symlink escape. Require cooperative cancellation, query deadlines, row and complete-result byte limits, predictable corrupt/non-SQLite errors, and deterministic connection cleanup on success, failure, cancellation, and Plugin disposal.

The independent Reviewer must inspect the full live diff and all applicable repository instructions and acceptance sources. Reproduce useful focused tests and gates; review SQLite authorization, path and symlink authority, statement completeness, parameter binding, numeric/BLOB serialization, result bounds, cancellation, connection lifecycle, model-visible schemas/results, package contents, generated catalogs, TypeScript aggregate and example wiring, bilingual docs, Agent Note, invariant, HMR disposal, real Loader composition, and assembled keyless snapshot evidence. Do not accept the predecessor Developer's reported 43 tests, build, type-check, lint, or gates without direct evidence. Report every P0-P3 finding. Any finding routes to Developer with reproducible evidence; Developer fixes and tests it, then routes back to Reviewer for a complete relevant re-review. A genuinely clean review routes to Validator.

Only after Reviewer is clean may the fresh Validator start. Agent-Team must freeze and install the reviewed `packages/storage/tool-sqlite` bundle in Validator's private DSH Profile before first activation. Validator directly calls `sqlite_schema` and `sqlite_query` through that managed Session and must not start nested DSH or manage tmux.

Validator independently creates a realistic workspace-local SQLite database with related `users`, `projects`, `tasks`, and `task_events` tables, foreign keys, indexes, a view, `NULL`, an integer beyond JavaScript's safe range, and BLOB data. Verify schema inspection, named and positional parameters, joins, aggregation, a CTE or window query, query-plan output, typed values, truncation, and cancellation/deadline behavior. Attempt DML, DDL, `ATTACH`, writable `PRAGMA`, multiple statements, and workspace/symlink escape; prove with file hashes and row counts that rejected operations leave the database unchanged. Verify frozen package identity, private Profile composition, durable model-visible tool-call/result evidence, and focused repository checks.

Validator is Completion Authority. Completion requires a clean Reviewer verdict plus successful real installation, load, direct invocation, independent result comparison, denial evidence, database immutability proof, durable trace evidence, and relevant tests. A source defect found after Validator's snapshot freezes must Block with exact evidence so another immutable continuation can be created.

Do not commit, push, publish, alter user-level DSH configuration, modify the original dirty `/Users/zengjice/Projects/deepseek-harness` worktree, or perform unrelated external actions.

Limits: at most 18 role Turns and 7200 seconds. After explicit disclosure that all three DSH Agents can access the host filesystem, environment credentials, and network without per-command approval, the user confirmed the proposed Reviewer continuation and its three-role `full-access` configuration. This confirmation applies only to this immutable Run.
