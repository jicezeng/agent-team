# DSH read-only SQLite Plugin case

Use three independent DeepSeek Harness External Agents in `/Users/zengjice/Projects/deepseek-harness-sqlite-e2e`: one Developer, one Reviewer, and one fresh Validator. Preserve the Developer and Reviewer Sessions. All three roles must use the evolving model identified in this natural-language task as `deepseek-official/doubao-seed-evolving`; Agent-Team Bootstrap extracts and freezes that explicit choice for each role, and every role verifies the observed runtime model evidence.

The Developer must implement an installable, product-quality, model-facing function Plugin named `@deepseek-ai/dsh-tool-sqlite` at `packages/storage/tool-sqlite`. It exposes `sqlite_schema` for inspecting workspace-authorized local SQLite tables, views, columns, primary keys, foreign keys, and indexes, plus `sqlite_query` for exactly one parameterized read-only `SELECT`, `WITH ... SELECT`, or `EXPLAIN QUERY PLAN` statement. Results must be typed and bounded, report elapsed time and explicit truncation, preserve integers that exceed JSON's safe range, and summarize BLOB values without returning their bytes.

Read-only behavior must be enforced by both the SQLite connection mode and an engine-level authorizer, never SQL text matching alone. Reject DML, DDL, `ATTACH`, `DETACH`, `VACUUM`, writable `PRAGMA`, extension loading, transactions, and multiple statements. Database paths must stay within the authorized workspace and resist absolute paths, parent traversal, and symlink escape. Implement cooperative cancellation, a query deadline, row and complete-result byte limits, predictable errors for corrupt or non-SQLite inputs, and deterministic connection cleanup on success, failure, cancellation, and Plugin disposal.

Follow all DeepSeek Harness repository instructions. The implementation must include focused unit and real SQLite integration tests, a non-unit real Loader composition, an assembled keyless model-visible snapshot, HMR disposal evidence, package and bilingual documentation, an Agent Note, invariant companion, generated tool/config catalogs, TypeScript aggregate and example wiring, and the smallest sufficient repository gates. Keep the Plugin opt-in rather than adding it to shipped defaults.

The independent Reviewer must inspect the complete live diff and acceptance sources, rerun useful focused checks, and report every P0-P3 finding. Review the SQLite authorization boundary, filesystem authority, symlink handling, statement completeness, parameter binding, numeric and BLOB serialization, result bounds, cancellation, connection lifecycle, model-visible schema and results, generated surfaces, documentation, and real-composition evidence. Any finding routes to the Developer; repeat Developer → Reviewer until no finding remains.

Only after a clean review, route to the fresh Validator. Agent-Team must freeze and install the reviewed `packages/storage/tool-sqlite` bundle in the Validator's private DSH Profile before its first activation. The Validator must call `sqlite_schema` and `sqlite_query` directly through that managed DSH Session and must not start a nested DSH or manage tmux.

The Validator independently creates a realistic workspace-local SQLite database containing related `users`, `projects`, `tasks`, and `task_events` tables, foreign keys, indexes, a view, `NULL`, an integer larger than JavaScript's safe integer range, and BLOB data. It must verify schema inspection, named and positional parameters, joins, aggregation, a CTE or window query, query-plan output, typed values, truncation, and at least one cancellation or deadline path. It must attempt DML, DDL, `ATTACH`, writable `PRAGMA`, multiple statements, and workspace/symlink escape, then independently prove with file hashes and row counts that every rejected operation left the database unchanged. It must also verify the frozen package identity, private Profile composition, durable model-visible tool-call/result evidence, and focused repository checks.

The Validator is Completion Authority. Completion requires a clean Reviewer verdict and successful real installation, load, direct invocation, independent result comparison, denial evidence, durable trace evidence, and relevant tests. A source defect found after the Validator snapshot freezes must Block with exact evidence so a continuation Run can install a new immutable snapshot.

Do not commit, push, publish, alter user-level DSH configuration, modify the original dirty `/Users/zengjice/Projects/deepseek-harness` worktree, or perform unrelated external actions.

Limits: at most 18 role turns and 7200 seconds. The user explicitly confirmed that all three External roles may use Agent-Team's `full-access` Profile for this Run.
