# DSH read-only SQLite Plugin continuation case

Continue the interrupted DSH SQLite Plugin E2E in the single Git worktree `/Users/zengjice/Projects/deepseek-harness-sqlite-e2e`. The user explicitly cancelled the latest predecessor Run `at-dsh-sqlite-plugin-e2e-cont-20260826` after its Developer Turn ended on a Provider `RATE_LIMIT`; that Run had continued the earlier cancelled Run `at-dsh-sqlite-plugin-e2e-v2-20260825`. Their uncommitted partial implementation remains at `packages/storage/tool-sqlite`, together with repository wiring, documentation, generated-catalog, lockfile, and TypeScript configuration changes. Treat the complete live diff as untrusted work material: inspect it directly, preserve useful work, fix defects, and finish the complete case rather than restarting blindly or accepting predecessor claims.

Use three independent DeepSeek Harness External Agents: a Developer, a Reviewer, and a fresh Validator. Developer and Reviewer use resumable Sessions within this continuation Run; Validator uses a fresh Session. All roles must use the explicitly selected model `deepseek-official/deepseek-v4-pro-ga-260813` and verify the observed runtime model evidence. Resolve its endpoint and credential only through DeepSeek Harness's native trusted environment; do not copy environment values into repository or Run artifacts.

The Developer must finish an installable, product-quality, model-facing function Plugin named `@deepseek-ai/dsh-tool-sqlite` at `packages/storage/tool-sqlite`. It exposes `sqlite_schema` for inspecting workspace-authorized local SQLite tables, views, columns, primary keys, foreign keys, and indexes, plus `sqlite_query` for exactly one parameterized read-only `SELECT`, `WITH ... SELECT`, or `EXPLAIN QUERY PLAN` statement. Results must be typed and bounded, report elapsed time and explicit truncation, preserve integers beyond JavaScript's safe range, and summarize BLOB values without returning their bytes.

Enforce read-only behavior with both the SQLite connection mode and an engine-level authorizer, never SQL text matching alone. Reject DML, DDL, `ATTACH`, `DETACH`, `VACUUM`, writable `PRAGMA`, extension loading, transactions, and multiple statements. Database paths must remain within the authorized workspace and resist absolute paths, parent traversal, and symlink escape. Implement cooperative cancellation, a query deadline, row and complete-result byte limits, predictable corrupt/non-SQLite errors, and deterministic connection cleanup on success, failure, cancellation, and Plugin disposal.

Follow every applicable DeepSeek Harness repository instruction. Complete focused unit and real SQLite integration tests, a non-unit real Loader composition, an assembled keyless model-visible snapshot, HMR disposal evidence, package and bilingual documentation, an Agent Note, invariant companion, generated tool/config catalogs, TypeScript aggregate and example wiring, and the smallest sufficient repository gates. The Plugin remains opt-in and must not enter shipped defaults. Generated build output or partial tests already present in the live diff are not proof until reproduced.

The independent Reviewer must inspect the full live diff and acceptance sources, rerun useful focused checks, and report every P0-P3 finding. Review SQLite authorization, filesystem and symlink authority, statement completeness, parameter binding, numeric/BLOB serialization, result bounds, cancellation, connection lifecycle, model-visible schemas/results, generated surfaces, docs, and real-composition evidence. Any finding routes to Developer; repeat Developer → Reviewer until no finding remains.

Only after a clean review may the fresh Validator start. Agent-Team must freeze and install the reviewed `packages/storage/tool-sqlite` bundle in Validator's private DSH Profile before its first activation. Validator calls `sqlite_schema` and `sqlite_query` directly through that managed Session; it must not start nested DSH or manage tmux.

Validator independently creates a realistic workspace-local SQLite database with related `users`, `projects`, `tasks`, and `task_events` tables, foreign keys, indexes, a view, `NULL`, an integer beyond JavaScript's safe range, and BLOB data. Verify schema inspection, named and positional parameters, joins, aggregation, a CTE or window query, query-plan output, typed values, truncation, and at least one cancellation/deadline path. Attempt DML, DDL, `ATTACH`, writable `PRAGMA`, multiple statements, and workspace/symlink escape; independently prove with file hashes and row counts that rejected operations leave the database unchanged. Also verify frozen package identity, private Profile composition, durable model-visible tool-call/result evidence, and focused repository checks.

Validator is Completion Authority. Completion requires a clean Reviewer verdict and successful real installation, load, direct invocation, independent result comparison, denial evidence, database immutability proof, durable trace evidence, and relevant tests. A source defect found after Validator's snapshot freezes must Block with exact evidence so another immutable continuation can be created.

Do not commit, push, publish, alter user-level DSH configuration, modify the original dirty `/Users/zengjice/Projects/deepseek-harness` worktree, or perform unrelated external actions.

Limits: at most 18 role Turns and 7200 seconds. After disclosure that every role can access the host filesystem, environment credentials, and network without per-command approval, the user explicitly confirmed this new SQLite Plugin Run's three-role `full-access` configuration. All three External roles may therefore use Agent-Team's `full-access` Profile for this immutable Run.
